function outCount(input1, input2) {
    let resout = input1 == 0 && input2 == 0 ? true : false;
    return resout;
}

function outScore(neko) {
    neko = Math.floor((neko * 1) / 2);
    return neko;
}
